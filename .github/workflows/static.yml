name: Vircadia Build and Deploy

on:
  push:
    branches: ["main"] # Hier den Namen deines Branches prüfen (meist main)

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Dependencies
        run: npm install

      - name: Build Project
        run: npm run build
        env:
          # Falls du spezielle Umgebungsvariablen hast, kommen sie hier hin
          NODE_ENV: production

      - name: Upload Build Folder
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist # GitHub nimmt jetzt nur den fertigen dist-Ordner

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
